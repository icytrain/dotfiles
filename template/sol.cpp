#include <bits/stdc++.h>
using namespace std;

using ll = long long;

using vi = vector<int>;
#define pb push_back
#define all(x) begin(x), end(x)
#define sz(x) (int)(x).size()

using pi = pair<int, int>;
#define fi first
#define se second
#define mp make_pair

#define E(i, a, b) for (int i = (a); i < (b); ++i)
#define F(i, a, b) E(i, a, (b) + 1)

int main() {
    cin.tie(0)->sync_with_stdio(0);
}